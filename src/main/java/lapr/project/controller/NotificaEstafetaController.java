package lapr.project.controller;
/*
public class NotificaEstafetaController {

    Scooter scoot;
    EstacionamentoScooter estac;

    public boolean checkWellDockedScooter(){
        AplicacaoPOT app = AplicacaoPOT.getInstance();
        SessaoUtilizador sessao = app.getSessaoAtual();
        String email = sessao.getEmail();
        Estafeta est = EstafetaDB.getEstafetaByEmail(email);
        Entrega ent = EntregaDB.getActiveEntrega(est);
        scoot = ent.getScooter();

        if(EstacionamentoScooterDB.getActiveParking(scoot) != null){
            estac = EstacionamentoScooterDB.getActiveParking(scoot);
            return true;
        }
        return false;
    }

    public double calcEstimativa(){
        double bateriaAtual = scoot.percentagemBateria;
        double bateriaNecessaria = 100 - bateriaAtual;
        return bateriaNecessaria; //DEFINIR CONSTANTE
    }
}
*/
